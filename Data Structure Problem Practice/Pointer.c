#include <stdio.h>
#include <stdlib.h>

int main() {
    int a,b;
    printf("Enter the number : ");
    scanf("%d%d",&a,&b);
    int sum = a + b;
    printf("Sum of two number : %d", sum);
    return 0;
}